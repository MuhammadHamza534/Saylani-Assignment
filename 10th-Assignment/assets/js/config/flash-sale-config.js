// Extracted from index.html to keep executable JavaScript out of HTML.
// Storefront maintainers may edit these values without changing feature code.
var FLASH_SHOW = "yes";              // ← "yes" = dikhe  |  "no" = chupe
      var FLASH_TEXT_LEFT = "⚡ Flash Sale — Limited Time Offer!";
      var FLASH_TEXT_RIGHT = "Ends Today — Don't Miss Out!";
      var FLASH_HOURS = 12;                 // ← timer ghante mein
